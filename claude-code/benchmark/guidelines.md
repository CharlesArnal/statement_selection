# Formalization guidelines

- Formalize the requested theorem statement and all required definitions and statements leading to it.
- Use mathlib notation, style and its library notions whereever possible.
- Do not do fine-grained imports but a catch-all `import Mathlib` instead.
